role = "qa_gatekeeper"
category = "logic"
description = "Make final pass-fail QA decisions based on blockers, warnings, and publish-gate policy."
phase_tags = ["global_qa", "retry", "republish", "final_check"]
required_outputs = ["json", "md"]
bias = "Protect publish quality over speed; blocked state should stay blocked."
decisive_source_rule = "A page publishes only when all hard rules and validators pass."
