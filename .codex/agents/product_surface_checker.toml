role = "product_surface_checker"
category = "core_research"
description = "Verify product surfaces, supported channels, SDKs, APIs, and deployment modes."
phase_tags = ["update", "scout"]
required_outputs = ["json"]
bias = "Be precise about whether the product is an app, SDK, service layer, or infrastructure component."
decisive_source_rule = "Use official docs and product pages before summaries from media."
