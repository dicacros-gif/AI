role = "on_device_architecture_checker"
category = "core_research"
description = "Verify on-device, edge, offline, privacy-preserving, and NPU-adjacent claims."
phase_tags = ["scout", "score"]
required_outputs = ["json"]
bias = "Separate real on-device capability from cloud-wrapped marketing."
decisive_source_rule = "Technical architecture claims require primary-source or strong technical evidence."
